package com.struts.action;

import java.util.Arrays;
import java.util.List;

import org.apache.struts2.convention.annotation.Action;
import org.apache.struts2.convention.annotation.Namespace;
import org.apache.struts2.convention.annotation.Result;
import org.apache.struts2.convention.annotation.ResultPath;

import com.opensymphony.xwork2.ActionSupport;
import com.struts.model.Employee;
import com.struts.model.Student;

@ResultPath(value = "/WEB-INF/content/studsuccess.jsp")
@Namespace("/")
@Action(value = "studregister",
results = { @Result(name="success",location ="studsuccess.jsp"),
		@Result(name="input",location ="studform.jsp")

})
public class StudentAction extends ActionSupport{

private static final long serialVersionUID = 1L;

	
	private Student student;
	private List<String> hobbies;

	
	public List<String> getHobbies() {
		return hobbies;
	}

	public void setHobbies(List<String> hobbies) {
		this.hobbies = Arrays.asList("sports","music","sleep");
	}

	public Student getStudent() {
		return student;
	}

	public void setStudent(Student student) {
		this.student = student;
	}

	
	
	
	@Override
	public String execute() throws Exception {
		// TODO Auto-generated method stub
		if(student.getStudentName().equals("prabhakar")) {
			return SUCCESS;
		}
		return INPUT;
	}
	
	
	@Override
	public void validate() {
		// TODO Auto-generated method stub
		if(student.getStudentName().length()<=0) {
			addFieldError("student.studentName", "Name is required");
		}
		if(student.getAge()<=20) {
			addFieldError("student.age", "Age should be graeter than 20");
		}
		if(student.getCity().length()<=0) {
			addFieldError("student.city", "city is required");
		}
		if(student.getAllowance()<=0) {
			addFieldError("student.employeeName", "salary should not be less than zero");
		}
		if(student.getEmail().length()<=0) {
			addFieldError("student.email", "email is required");
		}
		if(student.getCourse().length()<=0) {
			addFieldError("student.course", "course is required");
		}
		if(student.getRadio().length()<=0) {
			addFieldError("student.email", "email is required");
		}
		if(student.getEmail().length()<=0) {
			addFieldError("student.email", "email is required");
		}
	
	}

	
	
	
	
	
	

	

	
	
}
